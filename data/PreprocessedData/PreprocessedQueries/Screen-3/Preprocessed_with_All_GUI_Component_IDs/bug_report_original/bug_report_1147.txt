nav drawer selection color dark theme right summary the current selection color the navigation drawer select item status when the status screen show should darker currently not set part the dark theme navdrawer background color select dark use boil down set the theme style dynamically the layout the nav drawer item and have get work yet wip branch http github com barbeau gpstest tree nav drawer dark select here the main code application get prefs get get string string pref key dark theme layout inflate layout navdrawer item dark theme context context context theme wrapper get activity style nav drawer list item dark layout inflater layout inflater get activity get layout inflater clone context context view layout inflater inflate layout inflate container light theme view get activity get layout inflater inflate layout inflate container currently look like the below with only the area immediately surround the icon and text black the rest navdrawer background color select light which the color the light style set the xml step reproduce enable dark theme via setting open navigation drawer expect behavior select item navigation drawer should have darker highlight color make easier see should use navdrawer background color select dark observe behavior the lighter navdrawer background color select light color use the select item device and android version samsung galaxy android beta screenshot see above